import React from 'react';

const Message = (props) => {
    // const messages = 'Some text';
    return (
        <div>
            <p>{props.message}</p>
        </div>
    );

}

export default Message;
